# 参与贡献

感谢关注 Local 5E Campaign Tool。提交变更前请先阅读 [AGENTS.md](AGENTS.md)、[架构](docs/architecture.md)与相关领域文档。

## 基本流程

Windows 开发和自动化默认使用 PowerShell 7 与 Windows 工具路径。即使自动化接口名为 `bash`，
也应只用它启动 `pwsh`，不得默认把仓库命令转到 WSL 或 `/mnt/*` 路径；只有任务明确需要 Linux
语义时才例外说明并使用 WSL。

1. 从当前工作树和待处理问题开始，保留无关的用户修改。
2. 先检查最接近的生产代码、测试、迁移和稳定规则。
3. 为正常、边界、失败关闭、版本/幂等和事务失败行为添加聚焦测试。
4. 做最小且职责清晰的修改。
5. 先运行相关定向测试，再运行 `mvn clean verify`。
6. 生产代码或打包资源变化时审计 WAR。
7. 提交前运行 `git diff --check`，确认没有秘密、构建产物、备份、日志或真实存档。

## 兼容与范围

- 已应用迁移只增不改；新 schema 变更使用新的前向迁移。
- 不随类名或文档整理更改稳定 module key、字段/算法/事件键、摘要域、JNDI 名或环境变量合同。
- 产品目标是完整覆盖 SRD 5.1；每次变更应选择一个边界明确的规则领域切片，同时更新规则、服务、持久化、测试和当前实现说明。
- 现有 `dnd5e2014_srd51_se_v1` 是冻结的简化发布版。完整规则不得通过改写其定义、规范编码或摘要实现，必须使用新的发布身份和前向迁移。
- 完整规则族在跨领域发布候选前保持 `DRAFT`。DRAFT 可通过协调的前向迁移、代码、测试和文档继续演进；除非验收标准指出必须保留的真实数据，不为已废弃的 DRAFT 行为默认增加兼容层。
- 不在单个规则领域完成后冻结 archive/canonical 摘要或切换新战役默认值；角色、装备/冒险、战斗/状态、法术和怪物/魔法物品完成整合后再执行统一发布门。
- 产品保持 loopback-only、单 DM；LAN/公网、HTTPS、玩家账号、`/display`、`/api/public/*` 和公开投影不属于当前产品目标，除非另立范围并完成独立设计与安全评审。
- JDBC Repository 若由调用方拥有事务，不得自行提交或回滚。

问题与变更说明应聚焦可复现行为、预期结果和最小必要日志，避免包含私人战役数据或凭据。
